image: maven
 
before_script:
    - 'which ssh-agent || ( apt update -y && apt install openssh-client -y)'
    - eval $(ssh-agent -s)
    - ssh-add <(echo "$SSH_PRIVATE_KEY")
    - mkdir -p ~/.ssh
    - '[[ -f /.dockerenv ]] && echo -e "Host *\n\tStrictHostKeyChecking no\n\n" > ~/.ssh/config' 

stages:
    - build
    - deploy

build1:
    stage: build
    script:
      - cd BE/CustomerService/customers
      - mvn clean package 
    artifacts:
      paths:
      - BE/CustomerService/customers/target/*.jar
      expire_in: 1 day

build2:
    stage: build
    script:
      - cd BE/Gateway
      - mvn clean package 
    artifacts:
      paths:
      - BE/Gateway/target/*.jar
      expire_in: 1 day

deploy:
    stage: deploy
    script:
      - scp BE/CustomerService/customers/target/*.jar artemkulish123@35.226.70.206:/tmp
      - scp BE/Gateway/target/*.jar artemkulish123@35.226.70.206:/tmp
